// Placeholder from ticket 0.1 (monorepo scaffold). The real dashboard lands
// in Phase 1 (transcript viewer) through Phase 4 (analytics) — see docs/plan.md.
export function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>ScribeFlow</h1>
      <p>Dashboard scaffold — see docs/plan.md for the build roadmap.</p>
    </main>
  );
}
